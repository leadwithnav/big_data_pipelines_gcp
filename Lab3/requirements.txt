# Fix for Python 3.12: pkg_resources was removed, setuptools restores it
setuptools

# Apache Beam SDK with all Google Cloud dependencies (Pub/Sub, Dataflow, GCS, etc.)
# Using >=2.58.0 which is the first version with official Python 3.12 support
apache-beam[gcp]>=2.58.0
